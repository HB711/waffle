# Archive

This folder stores historical files that are useful for reference but should not be treated as the active code path.

Typical examples include:

- copied snapshots,
- GPT-generated drafts,
- old comparison baselines with unclear naming.

When a new script supersedes an older one, prefer moving the old file here instead of leaving it mixed into the active experiment directories.
